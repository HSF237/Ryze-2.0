"use client";import{products,money}from"../../../lib/store";import{useStore}from"../../../components/StoreProvider";import{Heart,ShoppingBag,Truck,ShieldCheck}from"lucide-react";export default function Product({params}:{params:{id:string}}){const p=products.find(x=>x.id===params.id)||products[0];const{add,toggleWish}=useStore();return <main className="productPage"><div className="bigProduct">{p.emoji}</div><div className="productInfo"><small>{p.category} / RYZE SELECT</small><h1>{p.name}</h1><div className="rating">★ {p.rating} · Preview reviews</div><p>{p.desc}</p><div className="productPrice">{money(p.price)} <del>{money(p.mrp)}</del></div><div className="stock">{p.stock} sample units in stock</div><div className="actions"><button className="primary" onClick={()=>add(p.id)}><ShoppingBag/>Add to cart</button><button className="wishBtn" onClick={()=>toggleWish(p.id)}><Heart/></button></div><div className="benefits"><span><Truck/>₹99 delivery · free over ₹2,999</span><span><ShieldCheck/>RYZE preview purchase protection</span></div></div></main>}