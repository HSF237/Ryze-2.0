"use client";import{useState}from"react";import{Search as S}from"lucide-react";import ProductGrid from"../../components/ProductGrid";import{products}from"../../lib/store";export default function Search(){const[q,setQ]=useState("");const f=products.filter(p=>(p.name+p.category).toLowerCase().includes(q.toLowerCase()));return <main className="page"><div className="searchPage"><S/><input autoFocus placeholder="What are you looking for?" value={q} onChange={e=>setQ(e.target.value)}/></div>{!q&&<div className="searchHints"><h2>Trending searches</h2><span>Wireless audio</span><span>Desk setup</span><span>Smart wearables</span><span>Under ₹2,999</span></div>}<ProductGrid items={f}/></main>}