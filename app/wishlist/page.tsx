"use client";import ProductGrid from"../../components/ProductGrid";import{products}from"../../lib/store";import{useStore}from"../../components/StoreProvider";export default function Wishlist(){const{wish}=useStore();return <main className="page"><div className="pageHero"><small>SAVED</small><h1>Your wishlist.</h1></div><ProductGrid items={products.filter(p=>wish.includes(p.id))}/></main>}